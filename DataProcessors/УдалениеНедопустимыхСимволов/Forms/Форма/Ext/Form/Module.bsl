
&НаКлиенте
Процедура Исправить(Команда)
	ИсправитьНаСервере();
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ИсправитьНаСервере()
	Выборка = Справочники.Номенклатура.Выбрать();
	Пока Выборка.Следующий() Цикл
	
		Символ = НайтиНедопустимыеСимволыXML(Выборка.Наименование);
		Если Символ <> 0 Тогда
		
			НоменклатураОбъект = Выборка.ПолучитьОбъект();
			НоменклатураОбъект.Удалить();
		
		КонецЕсли;
	
	КонецЦикла;
КонецПроцедуры
