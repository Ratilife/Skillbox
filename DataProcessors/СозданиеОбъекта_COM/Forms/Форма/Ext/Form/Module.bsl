
&НаКлиенте
Процедура Создать_СОМ_Объект(Команда)
    // Создание COM-объекта
    Попытка
        МояКомпонента = Новый COMОбъект("My1CExtension.My1CExtension");
        // Вызов метода ShowMessage
        МояКомпонента.ShowMessage("Hello from Python COM component!");
    Исключение
        // Обработка ошибки, если COM-объект не удалось создать или вызвать метод
        Предупреждение("Не удалось создать COM-объект или вызвать его метод: " + ОписаниеОшибки());
    КонецПопытки;

КонецПроцедуры
